import styled from "styled-components";
import styles from "./BackgroundCabbage";

export const StyledContainer = styled.div`
  position: absolute;
  bottom: 3%;
  left: 20%;
  background-color: ${styles.container};
`;
